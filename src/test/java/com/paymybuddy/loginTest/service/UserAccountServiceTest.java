package com.paymybuddy.loginTest.service;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class UserAccountServiceTest {
    /*
    @Autowired
    private UserAccountService userAccountService;

    @Autowired
    TransactionService transactionService;

    @Test
    public void getUserAccountsTest() {
        Iterable<UserAccount> users = userAccountService.getUserAccounts();
        UserAccount[] userArray = users.toArray(new UserAccount[0]);


        assertEquals("BON", userArray[0].getFirstname());
        assertEquals("buddy1@mail.com", userArray[0].getEmail());

    }
    */

}
